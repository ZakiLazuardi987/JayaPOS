// pos-init.js

// =========================================================
// INIT
// =========================================================
renderCart();
if (typeof renderCustomerInfo === 'function') {
    renderCustomerInfo();
}
